


далее:
	.вопрос об управлении scopes в Autofac:
		.когда и где создавать lifeTimeScope.
		.какие сервисы/объекты где размещать - singleInstanse/scoped.

	.либо - как обойтись без них.

	+ а действительно ли нужно управление scope-ами добавлять.

	+ продолжать ли добавления в игру - например локализацию.



Яну:

.дописать тесты для сохранения:
	.в тестах подделку для IGameDataService котоая:
		.даст фиксированные данные на старте.
		.через которую можно проверить сохраняемые на диск данные.



.https://autofac.org/ - разобрать как использовать модули:
	.заменить IoCBuilder.Building()
	.в интеграционных тестах добавить Autofac.
	.перебрать Builder:
		.какие методы ещё нужны? - после внедрения Autofac.
		.какие зависимости в конструкторе всё ещё нужны.
	.везде где идёт создание объектов через new Class() заменить на получение их из контейнера либо фабрики.


.добавить ещё один алгоритм Solwer-a:
	.на первом шаге поделить на 3 и спросить случайное, потом половинным делением.
		- на первом шаге деление на 3 и случайный выбор одного из отрезков.

.баг - если нет файла сохранения, то при попытке загрузки игр программа падает => должна показать что игр нету.
	.сообщение - нет игр для загрузки.	


.на потом (отложили):
	.перечитать про SOLID и методы из TDD (подделка, триангуляция, очевидная реализация).
