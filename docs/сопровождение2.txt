

вопросы:
	как связана картинка графа с кодом?
	
	зачем рисовать граф перед написанием кода?
	
	какую идею организации кода я протаскиваю?

	с наличием scope-ов Autofaca как поменялся подход к разработке кода?


из действий:
	.должен появиться ActiveGamePovider - откуда он появиться?

	.переделать GameLoader - разбить:
		.часть которая только получает список имён игр.
		.часть которая создаёт/сохраняет игру.

	.провести ревизию классов:
		.т.е. есть GameLoader и Builder - насколько у них разные ответственности?
		.может есть ещё что-то такое пересекающееся/лишнее.

	.переделать классы под использование ActiveGameService (его 2-х интерфейсов):
		.GameLoader.
		.Runner.
		.Builder.



влияние scope-ов Autofaca-ка:
	.строит объекты подставляя в них зависимости.
	.построенный объект храниться в скоупе и к нему можно получить доступ его запросив (если он не InstancePerDependency).


баг про выход за пределы масива сохранений индекса полученного от пользователя